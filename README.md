# inheritance in C#

- What is inheritance?
  A kind of relationship between two classes that allows one to inherit code from another one
- What are the benefits?
  Code re-use, polymorphic behaviour
- UML Notation
- Syntax